def is_palindrome(word):
    first_index = 0
    second_index = len(word)-1
    while first_index < second_index:
        if word(first_index) != word[second_index]
            return False
        first_index = first_index + 1
        second_index = second_index - 1
    return True
